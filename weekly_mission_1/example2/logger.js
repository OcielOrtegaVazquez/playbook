/* Esta es una funcion que se guardara en eeste modulo como informacion */
exports.info = (message) => {
    console.log(`info: ${message}`);
}

/* Esta es una funcion que se guardara como verbose */
exports.verbose = (message) => {
    console.log(`verbose: ${message}`);
}